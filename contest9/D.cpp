#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    int azul, N, vermelho;
    cin >> N >> azul;
    vermelho = N - azul;
    cout << N - azul + 1 << endl; // i = 1
    for (int i = 0; i < N; i++)
    {
        }
    return 0;
}